openapi: 3.1.0
info:
  title: Specialist Attendance Record
  version: 1.0.0
  description: Retrieves the specialist's own attendance record after the appointment time to confirm attended, missed, or unavailable evidence
servers:
  - url: https://medicotabs.onrender.com
paths:
  /api/yoxa/specialist-attendance-record:
    post:
      operationId: specialistAttendanceRecord
      summary: Retrieve specialist attendance record to confirm visit outcome
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required:
                - appointment_id
                - referral_id
                - attendance_status
              properties:
                appointment_id:
                  type: string
                  description: Appointment identifier
                referral_id:
                  type: string
                  description: Associated referral identifier
                patient_id:
                  type:
                    - string
                    - 'null'
                  description: Patient identifier
                specialist_id:
                  type:
                    - string
                    - 'null'
                  description: Specialist identifier
                attendance_status:
                  type: string
                  description: Attendance status from specialist record
                  enum:
                    - attended
                    - no_show
                    - cancelled
                    - rescheduled
                consultation_completed:
                  type: boolean
                  description: Whether consultation was completed
                consultation_notes:
                  type:
                    - string
                    - 'null'
                  description: Brief consultation summary
                follow_up_required:
                  type: boolean
                  description: Whether follow-up is needed
                attended_at:
                  type:
                    - string
                    - 'null'
                  description: Timestamp of attendance
      responses:
        '200':
          description: Specialist attendance record retrieved with visit outcome evidence
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required:
                  - record_id
                  - appointment_id
                  - attendance_status
                  - recorded_at
                properties:
                  record_id:
                    type: string
                  appointment_id:
                    type: string
                  referral_id:
                    type:
                      - string
                      - 'null'
                  attendance_status:
                    type: string
                  consultation_completed:
                    type: boolean
                  recorded_at:
                    type: string
                  recorded_by:
                    type:
                      - string
                      - 'null'
                  next_steps:
                    type:
                      - string
                      - 'null'

security:
  - bearerAuth: []

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
